# API
Contains the HTTP endpoints that clients (e.g., a Flutter app or external API consumers) interact with.

## Scope/purpose
- Handle requests from the frontend (GET, POST, PUT, DELETE, etc.).
- Validate request inputs.
- Call the appropriate services or business logic.
- Format and return responses (JSON/REST or WebSocket if real time).

## Future plan


## functions